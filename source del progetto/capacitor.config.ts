import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.newsappcris.angular',
  appName: 'NewsAppCris',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
